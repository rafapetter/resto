# Playwright E2E Test Configuration
# Copy this file to .env.e2e and fill in your values (never commit .env.e2e)

# Clerk — must use dev instance keys (testing tokens only work in dev)
CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...

# E2E test user — create this user in Clerk Dashboard → Users
# Enable email+password authentication in Clerk settings
E2E_CLERK_USER_EMAIL=e2e-test@yourapp.com
E2E_CLERK_USER_PASSWORD=YourStrongPassword123!
# Clerk user_id for this user (found in Clerk Dashboard → Users → click user → copy ID)
E2E_CLERK_USER_ID=user_2abc...

# App base URL — default is local dev server
PLAYWRIGHT_BASE_URL=http://localhost:3000

# Database — copy from your .env.local
# The test user must have signed in at least once so their tenant row exists
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require

# Required for the Next.js app to boot during E2E tests
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
ENCRYPTION_MASTER_KEY=32-char-secret-key-here-pad-it!
